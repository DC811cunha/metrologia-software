{
  "name": "softmeter-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Testes E2E (Playwright) do SoftMeter — fluxos ponta a ponta pela UI real, cruzando frontend e backend (ver plan.md).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
